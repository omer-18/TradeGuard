{
  "name": "kalshi-insider-trading",
  "version": "1.0.0",
  "description": "Kalshi Insider Trading Detection - Full Stack Application",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "kalshi-typescript": "^3.4.0",
    "mongodb": "^6.21.0"
  }
}
